{
  "name": "deleteme",
  "version": "1.0.0",
  "description": "",
  "main": "main.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start":"electron . ",
    "build":"electron-builder build --win --publish never",
    "deploy":"electron-builder build --win --publish always"
  },
  "author": "",
  "license": "ISC",
  "repository": {
    "type":"git",
    "url":"https://github.com/heshamt96/electronupdatetut.git"
  },
  "devDependencies": {
    "electron": "^18.2.0",
    "electron-builder": "^23.0.3"
  },
  
  "dependencies": {
    "electron-updater": "^5.0.1"
  }
}
